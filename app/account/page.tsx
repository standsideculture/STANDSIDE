'use client';
import Link from 'next/link';
import { useEffect, useState } from 'react';
export default function AccountPage(){const[order,setOrder]=useState<any>(null);useEffect(()=>{try{setOrder(JSON.parse(localStorage.getItem('standside-last-order')||'null'))}catch{}} ,[]);return <main className="min-h-screen bg-black px-5 py-10 text-white md:px-10"><div className="mx-auto max-w-5xl"><p className="text-xs tracking-[.3em] text-white/50">STANDSIDE / ACCOUNT</p><h1 className="mt-3 text-4xl font-black">MY ACCOUNT.</h1><div className="mt-8 grid gap-5 md:grid-cols-2"><section className="border border-white/10 p-6"><h2 className="font-bold">RECENT ORDER</h2>{order?<><p className="mt-5 text-sm">{order.name}</p><p className="mt-1 text-xs text-white/45">{order.phone}</p><p className="mt-5 text-sm font-bold">Rp {Number(order.total).toLocaleString('id-ID')}</p><p className="mt-2 text-xs text-white/45">{new Date(order.createdAt).toLocaleString('id-ID')}</p></>:<p className="mt-5 text-sm text-white/45">Belum ada order tersimpan di perangkat ini.</p>}</section><section className="border border-white/10 p-6"><h2 className="font-bold">QUICK ACTIONS</h2><div className="mt-5 flex flex-col gap-3"><Link href="/collection" className="border border-white/15 p-4 text-xs tracking-widest hover:border-white/50">SHOP COLLECTION</Link><Link href="/cart" className="border border-white/15 p-4 text-xs tracking-widest hover:border-white/50">VIEW CART</Link><Link href="/verify" className="border border-white/15 p-4 text-xs tracking-widest hover:border-white/50">VERIFY PRODUCT</Link></div></section></div></div></main>}
